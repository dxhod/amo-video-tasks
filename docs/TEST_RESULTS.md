# Фактичні перевірки

## Фінальна перевірка перед здачею

- Live 2026-09-08: web/worker `24d6394` успішно розгорнуті. [GitHub Actions](https://github.com/dxhod/amo-video-tasks/actions/runs/34200781076): lint/typecheck/unit/integration/усі E2E/build PASS.
- Railway worker: 1080p зі звуком (12 с) і джерело 4K без звуку (8 с), по 6 переставлених фрагментів — analyze/render succeeded з першої спроби. MP4 завантажені: 9155682 і 2989747 байтів. Жоден job не запускався локальним worker.
- Пошкоджений MP4: failed з першої спроби; error_logs містить normalize/INVALID_MEDIA, stack trace, user_id/task_id. Telegram API прийняв алерт з першої спроби: sent_at=2026-09-08T07:48:46.172337Z, приблизно через 2 с після помилки. Факт прочитання повідомлення людиною не перевірявся.
- Live admin: помилку видно; фільтр render прибирає запис, normalize + INVALID_MEDIA повертає його. Середній рендер показує 16.2 с для двох перевірочних задач.
- Артефакт: `.local/live-final-results.json`; проєкт «Перевірка перед здачею». Додаткові необов’язкові перевірки live TUS resume та restart посеред job залишаються непроведеними; відповідні локальні/CI сценарії пройшли.

- `24d6394`: 17 unit/FFmpeg tests PASS, 9 integration PASS (18.64 с), Docker browser workflow PASS (45.3 с; 48 с загалом).
- Додано реальне перетягування pointer, перестановку keyboard і перевірку збереження порядку після reload. Перша спроба тесту не прокручувала таймлайн у viewport; виправлено тест, змін UI не знадобилося.
- Рендер 1080p 12 с / 6 фрагментів у зворотному порядку до виправлення: FFmpeg завершено під 512 MiB. Після послідовного рендера: PASS, 360 кадрів, 28.6 с, cgroup peak 267296768 байтів.
- Джерело 4K 8 с → 1080p, 6 фрагментів у зворотному порядку: PASS, 240 кадрів, cgroup peak 318787584 байтів. Обидва заміри: Docker, 1 CPU, memory=512m, swap вимкнено; це ще не вимір live worker.
- Порядок звукових сигналів 880/440 Hz після перестановки: PASS. Шість однокадрових фрагментів зі звуком і без: PASS, 6 кадрів, декодування без помилок.
- Lint, typecheck, production build web/worker: PASS. Генеровані `.local` артефакти виключені з lint.

Стан на 2026-09-07, локальне Windows-середовище та Docker Linux. Цей журнал відокремлює виконані перевірки від підготовлених сценаріїв.

| Перевірка | Фактичний результат |
|---|---|
| `npm test` | PASS: 15 тестів, 3 файли; повторний запуск 16:53 UTC |
| Реальний FFmpeg | PASS: порядок кольорових сцен, точність кадрів, аудіо та його відсутність, вертикальне відео, пошкоджений файл, ліміт тривалості, timeout |
| Таймлайн | PASS: trim, split, reorder, delete, переведення часу, перевірка меж |
| Telegram із mock HTTP | PASS: успіх, помилка сервісу, відсутня конфігурація |
| `npm run typecheck` | PASS, включно з Auth E2E та виправленнями origin |
| `npm run lint` | PASS |
| `npm run build` | PASS: фінальна production-збірка web та worker |
| `npm audit` | 0 відомих вразливостей на момент встановлення зафіксованих залежностей |
| Візуальна перевірка | Оглянуто вхід, демо-проєкт, задачу; screenshot адмінки |
| Supabase integration | PASS: 5 сценаріїв, реальні Storage/pgmq/FFmpeg, 38.26 с |
| Playwright workflow, Auth та TUS | PASS спільним запуском: 3 тести, 3.7 хв; жодної помилки |
| Демонстраційний seed | PASS: два учасники, чотири задачі, два окремі рендери |
| TUS після обриву мережі | PASS: перервано PATCH після 6 MiB; після reload завантаження продовжено з ненульового offset, один asset отримав ready; 51.0 с |
| Очищення | PASS dry-run: незадіяних media старших 24 годин не знайдено; видалення не виконувалось |
| Docker worker | PASS: образ amo-worker:local зібрано; FFmpeg 5.1.9 запускається; worker підключений до локального Supabase |
| Docker web | PASS: образ amo-web:local зібрано, HTTP healthcheck 200, production Next.js запускається від користувача node |
| Docker workflow E2E | PASS: 1 тест, 25.0 с; лише контейнерний worker, вхід, upload, аналіз, монтаж v1/v2, скачування обох MP4, review та статистика |
| М’яке видалення: БД | PASS: 2 нові інтеграційні тести, 3.84 с; права, повторні запити, заборона змін, збереження історії, завершення worker після видалення |
| М’яке видалення: UI | PASS: розширений Docker workflow, 36.2 с; скасування, видалення, виключення зі статистики, відновлення, коментар і обидві версії збережені |
| Збірка після видалення | PASS: міграція 20260907090000, typecheck, lint, production Docker web; локальний контейнер оновлено |
| Railway та реальний Telegram | Не виконано: потрібні доступи й окремий дозвіл на зовнішні дії |

Локальний Docker має інший активний контейнер `solo-cluster-control-plane`; його не зупиняли. Storage спочатку не вклався в timeout CLI, згодом завершив запуск. Міграцію застосовано. Помилку кваліфікації SQL-змінної idempotency key виправлено у початковій міграції та локальній функції; інтеграційний suite пройшов.

Артефакти Playwright записуються в `playwright-report/`, `test-results/` та `.local/` і не включаються до git. Тести використовують лише локальні випадково створені облікові записи та синтетичні відео.
# Перевірка змін 2026-09-07: проєкти та повторний монтаж

- Міграція `20260907110000_projects_revisions.sql` застосована до локального Supabase зі збереженням наявних даних.
- `npm run typecheck`, `npm run lint`: успішно.
- Supabase integration: **9/9**, 16.56 с; включає справжній FFmpeg і завантаження обох історичних результатів тієї самої версії.
- Docker production build web: успішно; контейнер оновлено на `http://127.0.0.1:3100`.
- Docker Playwright workflow: **1/1**, 44.2 с (47.2 с із запуском).

| Сценарій | Очікування | Фактичний результат | Оброблено | Свідчення |
|---|---|---|---|---|
| Повторний монтаж тієї самої версії | Новий MP4, старий збережено | Обидва файли доступні | Так | `tests/integration/workflow.test.ts`, `tests/e2e/workflow.spec.ts` |
| Review/done або активний рендер | Редагування відхиляється | Перевірено БД та UI | Так | Ті самі тести |
| Застарілий результат | Передача на review вимагає нового рендера | Команда відхилена, UI показує пояснення | Так | Ті самі тести |
| Видалення проєкту учасником | Заборонено | Команда відхилена | Так | `tests/integration/projects.test.ts` |
| Видалений проєкт | Приховано задачі й коментарі команди | RLS повертає порожні результати | Так | `tests/integration/projects.test.ts` |
| Відновлення проєкту | Дані збережено, окремо видалені задачі лишаються видаленими | Перевірено | Так | `tests/integration/projects.test.ts`, Docker E2E |

На момент цих локальних перевірок live ще не існував; актуальна перевірка нижче.

## Live Railway Free — 2026-09-07

- Commit c70f56d, https://amo-web-production.up.railway.app; web і worker успішно запущені з GitHub main.
- `/api/health`, `/login`: HTTP 200 після виправлення domain target port на 8080.
- Окремий cloud Supabase: три міграції, приватні sources/media, анонімний користувач не бачить демо-задачу.
- Пряме завантаження синтетичного 640×360 відео зі звуком у cloud Storage; тільки Railway worker виконав analyze та два render jobs. Усі succeeded із першої спроби; render_ms 461 та 428.
- Два окремі MP4 по 6 секунд: 96 752 і 105 007 байтів; обидва завантажені для перевірки. V2 має зворотний порядок сцен.
- Через браузер: вхід виконавця, Kanban, вибір v1/v2, плеєри readyState=4 без media error; вхід окремого ревьюера, прийняття через web API, статус done, монтаж заблоковано.
- Моментальний показник пам’яті після рендера: worker ~108 MB, web ~194 MB при ліміті ~524 MB кожен. Це не вимір пікового споживання на важких файлах.
- Gmail SMTP увімкнено; стан збережено після перезавантаження Supabase Dashboard. Signup resend і password recovery для тестового Gmail-аліаса повернули успіх без SMTP-помилок. Власник скриньки підтвердив отримання обох листів і надав скриншот у розмові. Доставка: PASS; повний браузерний цикл реєстрації, підтвердження email, входу та відновлення пароля пройшов; старий пароль відхиляється, новий працює.
- Не перевірено в live: Telegram, TUS resume, рестарт посеред job, 1080p/4K. Локальні тести цих аспектів не видаються за live-перевірки.
- Деталі демо й час перевірки у `.local/live-smoke-results.json`; паролі лише в `.local/live-demo.json`.

## Історія та видалення версій — локально, 2026-09-08

- 17 unit/FFmpeg-тестів і 10 інтеграційних тестів пройшли.
- `tests/integration/version-history.test.ts`: збереження оригіналу та відрендерованого таймлайна, автоматична нова версія, конфлікт ревізії чернетки, повторний Render, заборона видалення оригіналу/активного job/версії на review, відмова сторонньому користувачу, вибір попередньої версії.
- `tests/e2e/workflow.spec.ts`: повний браузерний цикл із незалежними рендерами, копіюванням після рендера, видаленням через кошик і підтвердження, поверненням до оригінальних сцен, review другим користувачем; PASS (54,6 с).
- Міграції історії версій застосовано до live Supabase; Railway web/worker і CI перевірено після деплою.

## Явний вибір версії для review — локально, 2026-09-08

- Рендер не змінює статус; `task.status → review` приймає ID вибраної готової версії та атомарно фіксує `review_version_id`.
- Integration: на review доступні лише оригінал і вибрана попередня версія; її проміжні MP4 закриті RLS/Storage. Після повернення доступні всі версії; у done — лише прийнята.
- Browser E2E: вибір v1 після рендера v3, наявна кнопка в шапці, перевірка списків для виконавця й рев’юера та прийняття; PASS.
